# ChaeInG_project 작업 지침

## Secrets

Never read or inspect `.env` or `.env.*` files.

Do not use cat, grep, sed, awk, head, tail, less, more, python,
node, or any other command to access their contents.

If information from an environment file is required, ask me instead.
